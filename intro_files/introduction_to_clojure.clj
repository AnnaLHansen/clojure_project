;; Comments in Clojure starts with one semicolon. Depends on taste and code style.

(print-str "hello, World!") ; In the end of a line purhaps just one semicolon.
(+ 1 2 3)

;; REPL (Read, Eval, Print, Loop)
;; REPL is a way to execute code interactively
;; Nightcode has a project setting "Run with REPL"
;; One can also start REPL using leiningen on a terminal 'lein repl' 
;; user => (str "Hello World")
;; user => (+ 4 3)


